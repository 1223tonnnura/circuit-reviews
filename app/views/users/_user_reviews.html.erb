<ul class="list-unstyled">
  <% reviews.each do |review| %>
    <li class="media mb-3">
      <div class="offset-sm-2 media-body">
        <div class="col-sm-10 review-container">
          <h4 class="m-0"><%= link_to review.circuit.circuit, review.circuit %></h4>
          <p class="reviews-created"><%= review.created_at %></p>
          <h5 class="p-2 coment-container"><%= review.content %></h5>
        </div>
        <div class="pl-2">
          <% if current_user == review.user %>
            <%= link_to "削除", review, method: :delete, class: "btn btn-danger btn-sm" %>
          <% end %>
        </div>
      </div>
    </li>
  <% end %>
  <%= paginate @reviews %>
</ul>